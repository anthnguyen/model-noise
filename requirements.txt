# Core (needed everywhere, including mock dry runs on a laptop)
numpy>=1.26
scipy>=1.11
pandas>=2.1
pyarrow>=14.0
zarr>=2.16,<3
pyyaml>=6.0
scikit-learn>=1.3
matplotlib>=3.8
tqdm>=4.66

# GPU inference stack (cluster only) — uncomment or `uv pip install -e ".[gpu]"`
# torch>=2.2
# transformers>=4.44
# accelerate>=0.30
# bitsandbytes>=0.43
# sentencepiece

# Optional
# umap-learn>=0.5
# wandb
