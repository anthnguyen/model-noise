# MVP demo run 3/3: Qwen vs Gemma mixed-play, 50 rounds, no perturbation.
# Both models on one GPU: ~5GB (Qwen 4-bit) + ~7GB (Gemma-9B 4-bit).
condition: mixed
topic_id: t01
run:
  seed: 0
backend: hf
models:
  agent_a: {name: Qwen/Qwen2.5-7B-Instruct, quantization: 4bit, device: "cuda:0"}
  agent_b: {name: google/gemma-2-9b-it, quantization: 4bit, device: "cuda:0"}
conversation:
  max_turns: 50
  seed_string: "x7m3_k9p2_v4n1"
  framing: "This is a cipher. Discuss what it encodes."
perturbation:
  enabled: false
